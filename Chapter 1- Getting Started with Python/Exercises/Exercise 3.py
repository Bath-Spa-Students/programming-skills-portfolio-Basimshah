# We import the datetime module, which provides classes for working with dates and times.
import datetime

#We use datetime.datetime.now() to get the current date and time.
current_datetime = datetime.datetime.now()

#We print the current date and time to the console using the print statement.
print("The Current Date and Time is:", current_datetime)